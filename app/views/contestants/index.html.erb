<h3>
  <% @contestants.each do |contestant| %>
    <%= contestant.name %><br>
    <%= label :text, :projects, 'Projects: ' %>
    <%= contestant.projects.pluck(:name).join(', ') %><br><br>
  <% end %>
</h3>
